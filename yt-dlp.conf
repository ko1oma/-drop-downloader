# TikTok is currently sensitive to non-browser request fingerprints.
# Render installs curl-cffi in Dockerfile, so yt-dlp can use browser impersonation.
--impersonate chrome
--extractor-args tiktok:app_name=trill

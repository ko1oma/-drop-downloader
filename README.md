# Drop Downloader

Responsive public-media downloader frontend with a Node/Express API.

## Run locally

Requires Node.js, `yt-dlp`, and FFmpeg.

```bash
npm install
npm start
```

Open `http://localhost:3000`.

The service is limited to public TikTok and Instagram URLs and does not bypass private-account access controls.
